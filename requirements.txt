# v1.12: 모든 I/O 가 RS-485/아날로그(ADAM) — Pi GPIO 를 쓰지 않는다.
# 따라서 gpiozero/lgpio/rpi-lgpio/hx711-multi 는 제거됨.
# 순수 파이썬 의존성이라 Windows 개발과 Pi 배포가 동일하다.

# GUI (Python 3.14 는 cp310-abi3 wheel 로 설치됨)
PySide6==6.11.*

# RS-485 / Modbus RTU (ADAM 모듈, 실 하드웨어 모드에서만 실제 사용)
pymodbus>=3.7,<4
pyserial
